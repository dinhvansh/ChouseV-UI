type: minor

### Added
- **Advanced pipeline transforms** — calculated nodes now support allow-listed ClickHouse functions such as `lower`, `upper`, `trim`, `concat`, `coalesce`, `round`, `toDate`, and `toDateTime`, while locked versions expose a clear action to create the next editable draft.
